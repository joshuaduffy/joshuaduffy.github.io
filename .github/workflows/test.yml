name: Playwright Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  RUBY_VERSION: 3.4.2
  
jobs:
  test:
    uses: ./.github/workflows/playwright.yml
    with:
      ruby_version: ${{ github.env.RUBY_VERSION }}
